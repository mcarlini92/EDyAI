#ifndef __COLAS_H__#define __COLAS_H__#define MAX_COLA 100typedef struct _Cola {   	int data[MAX_COLA];	int front, back; } Cola;              #define   cola_data(l)     l->data#define   cola_front(l)    l->front#define   cola_back(l)     l->backCola *cola_create();int cola_queue(Cola *soyunpunterochachi); //devuelve el elemento de la primera posición (índice 0)Cola *cola_enqueue(Cola *ptro, int data); //agrega un elemento al final de la colaCola *cola_dequeue(Cola *ptro); //quita el primer elemento de la colavoid cola_print(Cola *ptro); //imprime la cola en ordenvoid cola_destroy(Cola *ptro); //destruye la cola#endif /*__COLAS_H__*/