#include <stdlib.h>#include "Stack.h"Stack *stack_create(){    Stack *newstack = malloc(sizeof(Stack));	stack_back(newstack) = -1;     	return newstack;}int stack_top(Stack *pila) {	int n = stack_back(pila);	return stack_data(pila)[n]}Stack *stack_push(Stack *pila, int dato){	int i = stack_back(pila);	stack_data(pila)[i+1] = dato;    stack_back(pila)++;      }Stack *stack_pop(Stack *pila) {	if(stack_back(pila) >= 0)		stack_back(pila)--;	return pila;}Stack *stack_reverse(Stack *pila){	Stack *revpila = stack_create();	int dato, nelemens;	while(stack_back(pila) >= 0){		nelemens = stack_back(pila);		aux = stack_data(pila)[nelemens];		revpila = stack_push(revpila, aux);		stack_back(pila)--;     	}	return revpila;}void stack_print(Stack *pila){	int i, n = stack_back(pila)	for (i=n ; i>=0 ; i--)		printf("%d ",stack_data(pila)[i]);}void stack_destroy(Stack *pila){	free(pila);}